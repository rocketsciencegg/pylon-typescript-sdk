node_modules/
wwwroot/*.js
typings/

# dist/ is intentionally committed — npm consumers installing this
# package via `github:` URL don't always get the `prepare` build hook
# to run (no devDeps hoisted into the nested install), so we ship
# compiled artifacts alongside the generated .ts sources.

# Generated by OpenAPI Generator (not useful, re-created on each build)
git_push.sh
.openapi-generator/
